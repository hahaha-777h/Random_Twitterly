<!-- 投稿一覧 -->
<h2 class="mb-3 fs-5">投稿一覧</h2>

<% @posts.each do |post| %>
<div class="card mb-2">
    <div class="card-body">
        <p class="mb-1"><%= post.content %></p>
        <small class="text-muted"><%= post.created_at.strftime("%Y-%m-%d %H:%M") %></small>
    </div>
</div>
<% end %>